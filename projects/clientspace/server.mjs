import {createServer} from 'node:http';import {readFile} from 'node:fs/promises';import {extname,relative,resolve} from 'node:path';import {fileURLToPath} from 'node:url';
const root=fileURLToPath(new URL('.',import.meta.url)),port=Number(process.env.PORT||3050),types={'.html':'text/html; charset=utf-8','.css':'text/css','.js':'text/javascript'};
createServer(async(req,res)=>{try{if(!['GET','HEAD'].includes(req.method))throw new Error('Method not allowed');const raw=decodeURIComponent(new URL(req.url,'http://localhost').pathname),path=raw==='/'?'/public/index.html':raw;if(!/^\/(public|src)\//.test(path)||path.includes('..'))throw new Error('Forbidden');const target=resolve(root,`.${path}`);if(relative(root,target).startsWith('..'))throw new Error('Forbidden');const file=await readFile(target);res.writeHead(200,{'content-type':types[extname(target)]||'text/plain','x-content-type-options':'nosniff','referrer-policy':'no-referrer','content-security-policy':"default-src 'self'; style-src 'self'; script-src 'self'; img-src 'self' data:"});res.end(req.method==='HEAD'?undefined:file)}catch{res.writeHead(404);res.end('Not found')}}).listen(port,'127.0.0.1',()=>console.log(`Clientspace: http://127.0.0.1:${port}`));
